{
  "name": "shadps4-trophy-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A self-hosted PS4 game and trophy tracker for ShadPS4 players.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "sync:games": "node scripts/sync-games.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/sync-games.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0"
  }
}
